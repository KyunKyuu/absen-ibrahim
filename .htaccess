<IfModule mod_rewrite.c>
    RewriteEngine On

    # Keep Laravel source outside the public request path while allowing the
    # repository to be deployed directly into Hostinger's public_html.
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
